梯仔向目标地点移动：
	1.移动过程：
		判断目标地点是否在窗口内（梯仔能否到达）move();
		a.可以    梯仔移动tiZaiMove();
		b.不可以  梯仔先移动到地图边缘tiZaiMove();
			  再移动背景地图使梯仔到达目标地点bgMove();
	2.到达目标地点：
		判断目标地点是否为答题点（由参数type控制）
		a.是（type = 1） 答题（与后台交互）
		b.否（type = 0） 前往下一个目标点